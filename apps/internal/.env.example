# internal (payroll payslips) — copy to .env. Being absorbed into apps/hr (MOD-07), then retired.
NEXT_PUBLIC_API_URL=http://localhost:3002
NEXT_PUBLIC_PORTAL_URL=http://localhost:3001
# Replaced by the `finance`/`hr` RBAC role in FND-07 — do not add new emails.
NEXT_PUBLIC_INTERNAL_AUTHORIZED_EMAILS=
