import { Metadata } from "next";
export declare function generateMetadata({ params, title, description, locale, imagePath, }: {
    params: {
        locale: string;
    };
    title: string;
    description: string;
    locale?: string;
    imagePath?: string;
}): Metadata;
//# sourceMappingURL=metadata.d.ts.map